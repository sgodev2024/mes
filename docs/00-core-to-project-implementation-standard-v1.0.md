# MES — Core-to-Project Standard Adoption v1.0

Dự án MES áp dụng bắt buộc, không sửa đổi chuẩn tại [core-to-project-implementation-standard-v1.0.md](core-to-project-implementation-standard-v1.0.md).

Baseline được khóa trong `CORE_BASELINE`. `origin` là repository MES; `upstream` là Java Core. Mọi ngoại lệ phải có ADR và phê duyệt trước khi triển khai.

